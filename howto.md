## How To

Run main.py such as:

```
python3 solver/main.py --path-to-pb "$PATH_TO_PROBLEM_FOLDER"
```

For the dummy problem, it should generate:

```json
{
  "actions": [
    "action1",
    "action3"
  ],
  "states_sequence": [
    "state1",
    "state2",
    "state3"
  ],
  "time": 11
}
```